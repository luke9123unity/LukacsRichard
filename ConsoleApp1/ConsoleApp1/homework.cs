/*//bool runLoop = true;
int loopcount = 0;
int sum = 0;

while (true)
{
    string line = Console.ReadLine();
    bool success = int.TryParse(line,out int number); //kimenetként megadja a line-ra vett eldöntendő értéket, siker esetén az out megadja a számot
    if (success)
    {
        sum += number;
        loopcount += 1;
    }
    else
    {
        //runLoop=false;
        break;
    }
    
}

float mean = (float)sum / loopcount;
Console.WriteLine($"átlag: {mean}"); //$ jel rövidített   összefűzés, értéket szöveg közbe illeszthetünk {} közé

double pi = Math.PI;

const float pi2 = 3.14f;
double val = Math.Pow(5, 6);
double sqrt = Math.Sqrt(56);

*/